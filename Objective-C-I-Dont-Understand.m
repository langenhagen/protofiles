@interface          // class interface declaration ...needs @end (?)
@protocol
@implementation     // interface implementation needs @end (?)
@class              // seems to be a forward declaration

@synthesize         // what is this?

__block             // ?:w


//What is the difference between Nullable and _Nullable ?


#pragma mark private         // does this have any meaning to the compiler?

(instancetype)              // what is this?


@interface AMSDataTagBuilder (CarloTags)                // what's the (CarloTags) ?
@implementation AMSDataTagBuilder (CarloTags)
